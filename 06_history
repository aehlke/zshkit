HISTFILE=~/.zsh_history
HISTSIZE=20480
SAVEHIST=10240

HISTFILE=~/.zsh_history
 
# Grep the history with 'h'
function h()
{
	history 0 | grep $1
}

# Grep the history with 'hs'
#hs () { if [[ ! -z $1 ]];then history 0 | grep $1;fi }

# vim: ft=zsh
